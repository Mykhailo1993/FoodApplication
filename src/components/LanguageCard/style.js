import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '45%',
    height: '31%',
    backgroundColor: 'white',
    marginHorizontal: 5,
    marginVertical: 5,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 12,
  },
  image: { width: 80, height: 80 },
  countryText: { marginTop: 50, fontSize: 18 },
});

export default styles;
